# Frontend Environment Variables
# API URL - use relative path in dev mode to leverage Vite proxy
# In production, set this to the full backend URL
VITE_API_URL=/api

# Server Hostname Configuration (for Vite dev server allowed hosts)
# Note: These are read at build/dev server start time, not runtime
SERVER_HOSTNAME=impact.vps
BACKEND_HOST=192.168.11.238
BACKEND_PORT=8000

# Session Timeout Settings (in minutes)
# IMPORTANT: VITE_SESSION_TIMEOUT_MINUTES must match backend ACCESS_TOKEN_EXPIRE_MINUTES
# for proper token expiration synchronization
VITE_SESSION_TIMEOUT_MINUTES=30
VITE_SESSION_WARNING_MINUTES=5
VITE_SESSION_REFRESH_THRESHOLD_MINUTES=10
