# IMPACT Application Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# ============================================
# DATABASE CONFIGURATION
# ============================================
# MongoDB connection URI
# Format: mongodb://[username:password@]host[:port]
MONGODB_URI=mongodb://localhost:27017

# Database names
MONGODB_DB_NAME=impact
MONGODB_SYSTEM_DB_NAME=impact_system

# ============================================
# SECURITY CONFIGURATION
# ============================================
# JWT Secret Key - MUST be changed from default
# Generate with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
# CRITICAL: Must be at least 32 characters
SECRET_KEY=CHANGE_ME_generate_with_python_secrets_module

# Encryption key for sensitive data (patient NHS numbers, MRNs)
# Generate with: python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
ENCRYPTION_KEY=CHANGE_ME_generate_with_cryptography_fernet

# ============================================
# API CONFIGURATION
# ============================================
# API server settings
API_HOST=0.0.0.0
API_PORT=8000
API_TITLE=IMPACT API
API_VERSION=1.1.0

# ============================================
# CORS CONFIGURATION
# ============================================
# Allowed CORS origins (comma-separated list)
# Add your frontend URLs here
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# CORS origin regex pattern (for network ranges)
# Example: Allow 192.168.10.0/24 and 192.168.11.0/24
CORS_ORIGIN_REGEX=http://192\.168\.(10|11)\.\d{1,3}:\d+

# ============================================
# ENVIRONMENT TYPE
# ============================================
# Options: development, staging, production
ENVIRONMENT=development

# ============================================
# LOGGING
# ============================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================
# BACKUP CONFIGURATION
# ============================================
# Backup directory (default: ~/.tmp/backups)
# BACKUP_DIR=/path/to/backups

# Backup retention (days)
# BACKUP_RETENTION_DAYS=30

# ============================================
# NOTES
# ============================================
# 1. After copying to .env, generate secure values for SECRET_KEY and ENCRYPTION_KEY
# 2. Never commit .env file to version control
# 3. Ensure SECRET_KEY is at least 32 characters
# 4. Keep encryption key secure - losing it means losing access to encrypted data
# 5. Update CORS_ORIGINS to match your frontend deployment URLs
